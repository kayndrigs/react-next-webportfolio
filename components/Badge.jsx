'use client';
import CountUp from 'react-countup';
i

const Badge = ({containerStyles, icon, endCountNum, endCountText, badgeTest}) => {
  return (
    <div className={`badge ${containerStyles}`}>Badge</div>
  )
}

export default Badge